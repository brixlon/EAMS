<div class="flex w-full min-h-screen">
  <%!-- SIDEBAR --%>
  <aside class="hidden md:block w-64 bg-indigo-600 text-white">
    <div class="p-6">
      <div class="flex items-center gap-2">
        <img src={~p"/images/logo.svg"} width="36" alt="EAMS Logo" class="brightness-0 invert" />
        <span class="font-bold text-xl">EAMS</span>
      </div>
    </div>
    
    <nav class="px-4 space-y-2">
      <a href={~p"/"} class="block py-2 px-3 rounded hover:bg-indigo-500 transition">
        🏠 Dashboard
      </a>
      <a href={~p"/organizations"} class="block py-2 px-3 rounded hover:bg-indigo-500 transition">
        🏢 Organizations
      </a>
      <a href={~p"/departments"} class="block py-2 px-3 rounded hover:bg-indigo-500 transition">
        🏛 Departments
      </a>
      <a href={~p"/teams"} class="block py-2 px-3 rounded hover:bg-indigo-500 transition">
        👥 Teams
      </a>
      <a href={~p"/attachees"} class="block py-2 px-3 rounded hover:bg-indigo-500 transition">
        🎓 Attachees
      </a>
      <a href={~p"/reports"} class="block py-2 px-3 rounded hover:bg-indigo-500 transition">
        📊 Reports
      </a>
    </nav>

    <%!-- Theme Toggle in Sidebar --%>
    <div class="px-4 py-6">
      <.theme_toggle />
    </div>
  </aside>

  <%!-- MAIN CONTENT AREA --%>
  <div class="flex-1 flex flex-col">
    <%!-- TOP HEADER/NAVBAR --%>
    <header class="bg-white border-b border-gray-200 px-8 py-4">
      <div class="flex items-center justify-between">
        <h1 class="text-2xl font-semibold text-gray-800">
          <%= assigns[:page_title] || "Dashboard" %>
        </h1>
        
        <%!-- Mobile menu button --%>
        <button class="md:hidden btn btn-ghost">
          <.icon name="hero-bars-3" class="size-6" />
        </button>
      </div>
    </header>

    <%!-- MAIN CONTENT --%>
    <main class="flex-1 p-8 overflow-y-auto bg-gray-50">
      <.flash_group flash={@flash} />
      <%= @inner_content %>
    </main>

    <%!-- FOOTER (Optional) --%>
    <footer class="bg-white border-t border-gray-200 px-8 py-4">
      <p class="text-sm text-gray-600 text-center">
        © <%= DateTime.utc_now().year %> EAMS - Enterprise Attachment Management System
      </p>
    </footer>
  </div>
</div>